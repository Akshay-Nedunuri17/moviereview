{
    "name": "moviereview-monorepo",
    "private": true,
    "scripts": {
        "build": "cd webfrontend && npm install && npm run build",
        "install-all": "npm install && cd backend && npm install && cd ../webfrontend && npm install && cd ../frontend && npm install"
    },
    "engines": {
        "node": "18.x"
    }
}